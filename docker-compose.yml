version: '2'
volumes:
  pdb:
services:
  web:
    build: .
    ports:
     - "3031:3031"
    depends_on:
     - db
    env_file: 
     - .env
  db:
    image: postgres:latest
    env_file:
     - .env
    volumes:
     - pdb:/var/lib/postgresql/data
